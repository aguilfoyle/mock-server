{
  "name": "ipsMock",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "node prestart.js",
    "start": "node prestart.js && node server.js",
    "start-nodemon": "node prestart.js && nodemon --watch mocks --ignore store.json server.js"
  },
  "author": "Scott Davis <scott@davisplanet.com> (https://github.com/bobum)",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.1",
    "express": "^4.15.4",
    "js-yaml": "^3.10.0",
    "mock-json-api": "^0.2.4",
    "nodemon": "^1.12.0",
    "yargs": "^8.0.2"
  },
  "devDependencies": {
    "request": "^2.81.0"
  }
}
